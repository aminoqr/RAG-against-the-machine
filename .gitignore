# Python
__pycache__/
*.pyc
.mypy_cache/
.pytest_cache/
.venv/
*.egg-info/

# uv
.python-version

# Data & generated artifacts (regenerated by `index`/`search_dataset`/`answer_dataset`,
# never committed per the subject's instructions)
# data/raw/
# data/processed/
# data/output/

# Model weights / caches downloaded at runtime
.cache/
*.safetensors
*.bin

# Moulinette binary (large, platform-specific; fetch/copy locally, don't commit)
# /moulinette

# Editor/OS
.DS_Store
.vscode/
.idea/

# NFS silly-rename artifacts (deleted-but-still-open files)
.nfs*
